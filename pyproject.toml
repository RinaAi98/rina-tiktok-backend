[project]
name = "rina-tiktok-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "flask[async]",
  "httpx",
]

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk",
]
