[project]
name = "rina-tiktok-backend"
version = "0.1.0"
description = "RINA TikTok Backend"
requires-python = ">=3.13"
dependencies = [
    "flask",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
